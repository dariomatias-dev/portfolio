import { Header } from "@/components/header";
import { HeroSection } from "@/components/hero-section";

const Home = () => {
  return (
    <main>
      <Header />

      <HeroSection />
    </main>
  );
};

export default Home;
